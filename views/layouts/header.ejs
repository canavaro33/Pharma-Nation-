<!DOCTYPE html>
<html lang="id">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= typeof pageTitle !=='undefined' ? pageTitle : 'Sistem Stok Obat' %>
    </title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.2/font/bootstrap-icons.css">
    <style>
        .sidebar {
            min-height: 100vh;
            background-color: #1F2937;
            color: white;
        }

        .sidebar a {
            color: #9CA3AF;
            text-decoration: none;
            padding: 10px 15px;
            display: block;
        }

        .sidebar a:hover,
        .sidebar a.active {
            background-color: #374151;
            color: white;
        }

        .content {
            padding: 20px;
            background-color: #F3F4F6;
            min-height: 100vh;
        }
    </style>
</head>

<body>
    <div class="d-flex">
        <!-- Sidebar -->
        <div class="sidebar col-md-2 d-none d-md-block p-3">
            <div class="d-flex align-items-center mb-4">
                <img src="/logo.png.png" alt="Logo" style="width: 40px; height: 40px; margin-right: 10px;">
                <h3 class="text-white fw-bold text-uppercase mb-0"
                    style="letter-spacing: 2px; color: #60A5FA !important;">
                    Pharma Nation
                </h3>
            </div>
            <nav>
                <a href="/dashboard"><i class="bi bi-speedometer2 me-2"></i> Dashboard</a>
                <a href="/obat/input"><i class="bi bi-plus-circle me-2"></i> Input Obat</a>
                <a href="/transaksi/fifo"><i class="bi bi-box-seam me-2"></i> Transaksi FIFO</a>
                <a href="/obat/list"><i class="bi bi-list-ul me-2"></i> Medicine List</a>
                <a href="/laporan/stock"><i class="bi bi-file-earmark-bar-graph me-2"></i> Stock Report</a>
                <a href="/donations"><i class="bi bi-gift me-2"></i> Donations</a>
                <a href="/logout" class="text-danger mt-5"><i class="bi bi-box-arrow-right me-2"></i> Logout</a>
            </nav>
        </div>

        <!-- Main Content -->
        <div class="content col-md-10 col-12">
            <nav class="navbar navbar-light bg-white mb-4 shadow-sm rounded p-3">
                <div class="container-fluid">
                    <span class="navbar-brand mb-0 h1">
                        <%= typeof pageTitle !=='undefined' ? pageTitle : 'Apotek' %>
                    </span>
                </div>
            </nav>